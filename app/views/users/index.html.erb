
<% @user.each do |user| %>


 
      <div class="border-form-div-1">
        <h2><%= link_to user.firstname + " " + user.name, user_path(user) %></h2>
        <p> <%= user.bund + " | " + "Aktiv seit: " + user.aktiv_seit.strftime("%d.%m.%Y").to_s %> 
      </div>

  
<% end %>
